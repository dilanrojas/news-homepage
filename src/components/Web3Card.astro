<div class="web3-image">
  <img src="/image-web-3-desktop.jpg" alt="Imagen de Web 3">
</div>

<div class="web3-title">
  <h1>The Bright Future Of Web 3.0?</h1>
</div>

<div class="web3-description">
  <p>
    We dive into the next evolution of the web that claims
    to put the power of the platforms back into the hands
    of the people. But is it really fulfilling its promise?
  </p>
  <button>Read more</button>
</div>

<style>
  .web3-image {
    grid-column: 1 / 3;
  }

  .web3-image img {
    width: 100%;
    height: auto;
    aspect-ratio: 70/30;
  }

  .web3-title {
    grid-row: 2;
    font-size: var(--fs-800);
  }

  .web3-description {
    grid-row: 2;
    grid-column: 2;
    display: flex;
    flex-direction: column;
  }

  .web3-description p {
    margin-bottom: auto;
    color: var(--clr-dark-gray-blue);
  }

  .web3-description button {
    border: 0;
    cursor: pointer;
    color: var(--clr-off-white);
    font-weight: var(--fw-semibold);
    background-color: var(--clr-soft-red);
    text-transform: uppercase;
    letter-spacing: 3px;
    width: 180px;
    height: 40px;
    transition: background-color .06s ease-out;
  }

  .web3-description button:hover {
    background-color: var(--clr-dark-blue);
  }

  @media(max-width: 1040px) {
    .web3-description p {
      padding-bottom: 2em;
    }
  }

</style>